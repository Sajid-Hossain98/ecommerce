import useFirestoreCollection from "../../hooks/useFirestoreCollection";

const CustomBuilds = () => {
  const productCollection = useFirestoreCollection("");

  return <div>CustomBuilds</div>;
};

export default CustomBuilds;
